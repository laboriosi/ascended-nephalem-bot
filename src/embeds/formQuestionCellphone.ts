import { EmbedBuilder } from "discord.js";

export default new EmbedBuilder()
  .setTitle("Qual é o número do seu celular?")
  .setColor("#da373c")
  .setDescription(
    `Precisamos dessa informação porque também temos um grupo no whatsapp; dessa forma conseguimos evitar que entre pessoas que não são da guild ou caso necessário remover pessoas que já não estão mais na guild`
  );

import { EmbedBuilder } from "discord.js";

export default new EmbedBuilder()
  .setTitle("Formulário de Recrutamento")
  .setColor("#da373c")
  .setDescription(
    `Torne-se um membro da The Order! 

  Ao clicar no botão abaixo será criado um novo canal de texto, onde será iniciado o formulário de recrutamento.`
  );

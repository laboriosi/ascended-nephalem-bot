import { EmbedBuilder } from "discord.js";

export default new EmbedBuilder()
  .setTitle("Compartilhe seu grupo aqui!")
  .setColor("#da373c")
  .setDescription(
    `Após criar seu grupo, seleciona uma opção para convidar outros membros da guild para jogar com você.`
  );

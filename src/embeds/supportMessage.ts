import { EmbedBuilder } from "discord.js";

export default new EmbedBuilder()
  .setTitle("Suporte")
  .setColor("#da373c")
  .setDescription(
    `Clique no botão abaixo para entrar em contato com a coordenação da Ascended Nephalem a respeito de uma sugestão ou denúncia`
  );
